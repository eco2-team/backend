# ============================================================================
# Info API Routing - Production
# ============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod-route-info
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '51'
  labels:
    tier: network
    domain: info
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: prod
  source:
    repoURL: https://github.com/eco2-team/backend.git
    targetRevision: main
    path: workloads/routing/info/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: info
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
