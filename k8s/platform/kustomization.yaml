# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Platform Kustomization
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Wave 10: 플랫폼 서비스 (Tier 1)
# - AWS Load Balancer Controller (ALB 자동 생성)
# - cert-manager (TLS 인증서 관리) - 향후 추가
# - ingress-nginx (향후 추가)
#
# 참고: 현재는 Ansible에서 ALB Controller, Cert-Manager를 설치
#       향후 ArgoCD로 완전 이관 예정
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # 향후 추가 예정:
  # - alb-controller/
  # - cert-manager/
  # - ingress-nginx/
  
  # 현재는 Ansible에서 관리

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: ecoeco-platform
  app.kubernetes.io/component: platform

