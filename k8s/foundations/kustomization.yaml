# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Foundations Kustomization
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Wave -1: 최우선 실행
# - CRDs (Prometheus, Cert-Manager, External Secrets, etc.)
# - Namespaces (Tier 기반 도메인 구조)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # 1. Namespaces 먼저 생성
  - ../namespaces/domain-based.yaml
  
  # 2. CRDs (향후 추가)
  # - crds/prometheus-operator.yaml
  # - crds/cert-manager.yaml
  # - crds/external-secrets.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: ecoeco-platform
  app.kubernetes.io/component: foundations

