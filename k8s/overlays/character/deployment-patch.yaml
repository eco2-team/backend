apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 2
  template:
    spec:
      nodeSelector:
        domain: character
      tolerations:
      - key: domain
        operator: Equal
        value: character
        effect: NoSchedule
      containers:
      - name: app
        env:
        - name: SERVICE_NAME
          value: "character-api"
        - name: REDIS_HOST
          value: "redis.data.svc.cluster.local"
        - name: POSTGRES_HOST
          value: "postgresql.data.svc.cluster.local"
