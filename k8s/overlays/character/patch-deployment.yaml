apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 2
  template:
    metadata:
      labels:
        tier: business-logic
    spec:
      containers:
        - name: app
          image: ghcr.io/sesacthon/character-api:latest
          env:
            - name: SERVICE_NAME
              value: character
            - name: SERVICE_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
