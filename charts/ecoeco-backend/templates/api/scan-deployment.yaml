{{- if .Values.api.scan.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scan-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: scan-api
    component: api
    domain: scan
    phase: "2"
    {{- include "ecoeco-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.scan.replicas }}
  selector:
    matchLabels:
      app: scan-api
  template:
    metadata:
      labels:
        app: scan-api
        component: api
        domain: scan
        phase: "2"
    spec:
      {{- if .Values.api.scan.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.api.scan.nodeSelector | nindent 8 }}
      {{- end }}
      containers:
      - name: api
        image: "{{ .Values.api.scan.image.repository }}:{{ .Values.api.scan.image.tag | default .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.api.scan.port }}
          protocol: TCP
        env:
        {{- range .Values.api.scan.env }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        - name: DATABASE_URL
          value: "postgresql://{{ .Values.secrets.postgresql.username }}:{{ .Values.secrets.postgresql.password }}@{{ .Values.externalServices.postgresql.host }}:{{ .Values.externalServices.postgresql.port }}/{{ .Values.externalServices.postgresql.database }}"
        - name: REDIS_URL
          value: "redis://{{ .Values.externalServices.redis.host }}:{{ .Values.externalServices.redis.port }}/0"
        - name: S3_BUCKET
          value: "{{ .Values.externalServices.s3.bucket }}"
        - name: S3_REGION
          value: "{{ .Values.externalServices.s3.region }}"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.api.scan.resources | nindent 10 }}

---
apiVersion: v1
kind: Service
metadata:
  name: scan-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: scan-api
    {{- include "ecoeco-backend.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
  - port: {{ .Values.api.scan.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: scan-api
{{- end }}

