{{- if .Values.api.character.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: character-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: character-api
    component: api
    domain: character
    phase: "2"
    {{- include "ecoeco-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.character.replicas }}
  selector:
    matchLabels:
      app: character-api
  template:
    metadata:
      labels:
        app: character-api
        component: api
        domain: character
        phase: "2"
    spec:
      {{- if .Values.api.character.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.api.character.nodeSelector | nindent 8 }}
      {{- end }}
      containers:
      - name: api
        image: "{{ .Values.api.character.image.repository }}:{{ .Values.api.character.image.tag | default .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.api.character.port }}
          protocol: TCP
        env:
        {{- range .Values.api.character.env }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        - name: DATABASE_URL
          value: "postgresql://{{ .Values.secrets.postgresql.username }}:{{ .Values.secrets.postgresql.password }}@{{ .Values.externalServices.postgresql.host }}:{{ .Values.externalServices.postgresql.port }}/{{ .Values.externalServices.postgresql.database }}"
        - name: REDIS_URL
          value: "redis://{{ .Values.externalServices.redis.host }}:{{ .Values.externalServices.redis.port }}/0"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.api.character.resources | nindent 10 }}

---
apiVersion: v1
kind: Service
metadata:
  name: character-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: character-api
    {{- include "ecoeco-backend.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
  - port: {{ .Values.api.character.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: character-api
{{- end }}

