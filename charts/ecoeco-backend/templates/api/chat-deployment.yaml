{{- if .Values.api.chat.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: chat-api
    component: api
    domain: chat
    phase: "3"
    {{- include "ecoeco-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.chat.replicas }}
  selector:
    matchLabels:
      app: chat-api
  template:
    metadata:
      labels:
        app: chat-api
        component: api
        domain: chat
        phase: "3"
    spec:
      {{- if .Values.api.chat.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.api.chat.nodeSelector | nindent 8 }}
      {{- end }}
      containers:
      - name: api
        image: "{{ .Values.api.chat.image.repository }}:{{ .Values.api.chat.image.tag | default .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.api.chat.port }}
          protocol: TCP
        env:
        {{- range .Values.api.chat.env }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        - name: DATABASE_URL
          value: "postgresql://{{ .Values.secrets.postgresql.username }}:{{ .Values.secrets.postgresql.password }}@{{ .Values.externalServices.postgresql.host }}:{{ .Values.externalServices.postgresql.port }}/{{ .Values.externalServices.postgresql.database }}"
        - name: REDIS_URL
          value: "redis://{{ .Values.externalServices.redis.host }}:{{ .Values.externalServices.redis.port }}/0"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: openai-credentials
              key: api-key
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.api.chat.resources | nindent 10 }}

---
apiVersion: v1
kind: Service
metadata:
  name: chat-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: chat-api
    {{- include "ecoeco-backend.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
  - port: {{ .Values.api.chat.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: chat-api
{{- end }}

