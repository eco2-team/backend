{{- if .Values.workers.gpt5Analyzer.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpt5-analyzer
  namespace: {{ .Values.namespaces.workers }}
  labels:
    app: gpt5-analyzer
    component: worker
    queue: q.gpt5_analysis
    {{- include "growbin-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.workers.gpt5Analyzer.replicas }}
  selector:
    matchLabels:
      app: gpt5-analyzer
  template:
    metadata:
      labels:
        app: gpt5-analyzer
        component: worker
        queue: q.gpt5_analysis
    spec:
      {{- with .Values.workers.gpt5Analyzer.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: worker
        image: "{{ .Values.workers.gpt5Analyzer.image.repository }}:{{ .Values.workers.gpt5Analyzer.image.tag | default .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        command:
          {{- toYaml .Values.workers.gpt5Analyzer.command | nindent 10 }}
        env:
        {{- range .Values.workers.common.env }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        {{- range .Values.workers.gpt5Analyzer.env }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        resources:
          {{- toYaml .Values.workers.gpt5Analyzer.resources | nindent 10 }}
{{- end }}

