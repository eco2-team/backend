{{- if .Values.api.location.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: location-api
    component: api
    domain: location
    {{- include "growbin-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.location.replicas }}
  selector:
    matchLabels:
      app: location-api
  template:
    metadata:
      labels:
        app: location-api
        component: api
        domain: location
    spec:
      {{- if .Values.api.location.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.api.location.nodeSelector | nindent 8 }}
      {{- end }}
      containers:
      - name: api
        image: "{{ .Values.api.location.image.repository }}:{{ .Values.api.location.image.tag | default .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.api.location.port }}
          protocol: TCP
        env:
        {{- range .Values.api.location.env }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        - name: DATABASE_URL
          value: "postgresql://{{ .Values.secrets.postgresql.username }}:{{ .Values.secrets.postgresql.password }}@{{ .Values.externalServices.postgresql.host }}:{{ .Values.externalServices.postgresql.port }}/{{ .Values.externalServices.postgresql.database }}"
        - name: REDIS_URL
          value: "redis://{{ .Values.externalServices.redis.host }}:{{ .Values.externalServices.redis.port }}/0"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.api.location.resources | nindent 10 }}

---
apiVersion: v1
kind: Service
metadata:
  name: location-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: location-api
    {{- include "growbin-backend.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
  - port: {{ .Values.api.location.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: location-api
{{- end }}

