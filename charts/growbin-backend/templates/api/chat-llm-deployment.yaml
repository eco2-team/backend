{{- if .Values.api.chatLlm.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-llm-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: chat-llm-api
    component: api
    domain: chat
    {{- include "growbin-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.chatLlm.replicas }}
  selector:
    matchLabels:
      app: chat-llm-api
  template:
    metadata:
      labels:
        app: chat-llm-api
        component: api
        domain: chat
    spec:
      {{- with .Values.api.common.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: api
        image: "{{ .Values.api.chatLlm.image.repository }}:{{ .Values.api.chatLlm.image.tag | default .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.api.chatLlm.port }}
          protocol: TCP
        env:
        {{- range .Values.api.chatLlm.env }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        - name: DATABASE_URL
          value: "postgresql://{{ .Values.secrets.postgresql.username }}:{{ .Values.secrets.postgresql.password }}@{{ .Values.externalServices.postgresql.host }}:{{ .Values.externalServices.postgresql.port }}/{{ .Values.externalServices.postgresql.database }}"
        - name: REDIS_URL
          value: "redis://{{ .Values.externalServices.redis.host }}:{{ .Values.externalServices.redis.port }}/0"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          {{- toYaml .Values.api.chatLlm.resources | nindent 10 }}

---
apiVersion: v1
kind: Service
metadata:
  name: chat-llm-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: chat-llm-api
    {{- include "growbin-backend.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
  - port: {{ .Values.api.chatLlm.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    chat-llm-api
{{- end }}

