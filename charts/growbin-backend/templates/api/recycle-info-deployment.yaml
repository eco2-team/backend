{{- if .Values.api.recycleInfo.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recycle-info-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: recycle-info-api
    component: api
    domain: recycle
    {{- include "growbin-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.recycleInfo.replicas }}
  selector:
    matchLabels:
      app: recycle-info-api
  template:
    metadata:
      labels:
        app: recycle-info-api
        component: api
        domain: recycle
    spec:
      {{- with .Values.api.common.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: api
        image: "{{ .Values.api.recycleInfo.image.repository }}:{{ .Values.api.recycleInfo.image.tag | default .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.api.recycleInfo.port }}
          protocol: TCP
        env:
        {{- range .Values.api.recycleInfo.env }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        - name: DATABASE_URL
          value: "postgresql://{{ .Values.secrets.postgresql.username }}:{{ .Values.secrets.postgresql.password }}@{{ .Values.externalServices.postgresql.host }}:{{ .Values.externalServices.postgresql.port }}/{{ .Values.externalServices.postgresql.database }}"
        - name: REDIS_URL
          value: "redis://{{ .Values.externalServices.redis.host }}:{{ .Values.externalServices.redis.port }}/0"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          {{- toYaml .Values.api.recycleInfo.resources | nindent 10 }}

---
apiVersion: v1
kind: Service
metadata:
  name: recycle-info-api
  namespace: {{ .Values.namespaces.api }}
  labels:
    app: recycle-info-api
    {{- include "growbin-backend.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
  - port: {{ .Values.api.recycleInfo.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: recycle-info-api
{{- end }}

