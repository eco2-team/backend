# RabbitMQ Kustomization
# Message Queue (Tier 3)
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Placeholder: Ansible이 RabbitMQ Operator로 배포
  # 향후 ArgoCD로 전환 시:
  # - rabbitmqcluster.yaml

commonLabels:
  app.kubernetes.io/name: rabbitmq
  app.kubernetes.io/part-of: ecoeco-platform
  app.kubernetes.io/component: mq
  tier: messaging

namespace: messaging

