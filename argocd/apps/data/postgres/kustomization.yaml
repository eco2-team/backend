# PostgreSQL Kustomization
# Primary Database (Tier 4)
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Placeholder: Ansible이 PostgreSQL StatefulSet 배포
  # 향후 Operator CR로 전환 시:
  # - postgresql.yaml (Operator CR)
  # - servicemonitor.yaml

commonLabels:
  app.kubernetes.io/name: postgresql
  app.kubernetes.io/part-of: ecoeco-platform
  app.kubernetes.io/component: database
  tier: data

namespace: data

