# ArgoCD Application: Auth API
# Wave 60: Phase 1 Core API
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-api
  namespace: argocd
  labels:
    app: auth-api
    domain: auth
    phase: "1"
    tier: business-logic
  annotations:
    argocd.argoproj.io/sync-wave: "60"  # Phase 1
spec:
  project: default
  
  # Source: Kustomize overlay
  source:
    repoURL: https://github.com/SeSACTHON/backend
    targetRevision: develop
    path: k8s/overlays/auth
  
  # Destination
  destination:
    server: https://kubernetes.default.svc
    namespace: auth
  
  # Sync Policy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
    
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
  
  revisionHistoryLimit: 5

