apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: externalsecrets.external-secrets.io
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: dev-external-secrets-webhook
          namespace: platform-system
          path: /convert
          port: 443
      conversionReviewVersions:
      - v1
