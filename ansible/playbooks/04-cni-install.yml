- name: CNI 플러그인 설치 및 클러스터 검증 (Calico VXLAN Always, BGP Off)
  hosts: masters
  become: yes
  become_user: "{{ kubectl_user }}"
  gather_facts: no
  tasks:
    - name: CNI 설치 및 검증 작업 포함
      ansible.builtin.import_tasks: tasks/cni-install.yml

