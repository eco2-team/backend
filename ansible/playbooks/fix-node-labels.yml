- name: Kubelet 노드 라벨 수정 (Kubernetes 1.24+ 호환)
  hosts: all
  become: yes
  gather_facts: yes
  vars:
    migrate_node_labels: true
  tasks:
  - name: Sesacthon 표준 라벨/테인트 적용
    ansible.builtin.import_tasks: tasks/fix-node-labels.yml
