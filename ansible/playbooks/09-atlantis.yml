# Atlantis 설치 (Terraform GitOps)
# 14-Node Architecture: Monitoring 노드에 배포
---
- name: Atlantis 설치 및 설정
  hosts: masters
  become: yes
  become_user: "{{ kubectl_user }}"
  gather_facts: yes
  tasks:
    - name: "Atlantis namespace 생성"
      shell: |
        kubectl create namespace atlantis --dry-run=client -o yaml | kubectl apply -f -
      register: atlantis_namespace
      changed_when: "'created' in atlantis_namespace.stdout"

    - name: "Atlantis Secret 확인"
      shell: |
        kubectl get secret atlantis-secrets -n atlantis 2>/dev/null || echo "NOT_FOUND"
      register: atlantis_secret_check
      changed_when: false

    - name: "Atlantis Secret 생성 안내 (수동 생성 필요)"
      debug:
        msg:
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - "⚠️  Atlantis Secret을 수동으로 생성해야 합니다"
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - ""
          - "필요한 Secret:"
          - "  1. github-token: GitHub Personal Access Token"
          - "     - 권한: repo, admin:repo_hook"
          - "     - 생성: GitHub Settings → Developer settings → Personal access tokens"
          - ""
          - "  2. github-webhook-secret: Webhook Secret (랜덤 문자열)"
          - "     - 생성: openssl rand -hex 20"
          - ""
          - "  3. aws-access-key-id: AWS Access Key ID"
          - "  4. aws-secret-access-key: AWS Secret Access Key"
          - ""
          - "Secret 생성 명령어:"
          - "  kubectl create secret generic atlantis-secrets -n atlantis \\"
          - "    --from-literal=github-token='YOUR_GITHUB_TOKEN' \\"
          - "    --from-literal=github-webhook-secret='YOUR_WEBHOOK_SECRET' \\"
          - "    --from-literal=aws-access-key-id='YOUR_AWS_ACCESS_KEY_ID' \\"
          - "    --from-literal=aws-secret-access-key='YOUR_AWS_SECRET_ACCESS_KEY'"
          - ""
          - "또는 파일에서 생성:"
          - "  kubectl create secret generic atlantis-secrets -n atlantis \\"
          - "    --from-file=github-token=/path/to/github-token.txt \\"
          - "    --from-file=github-webhook-secret=/path/to/webhook-secret.txt \\"
          - "    --from-file=aws-access-key-id=/path/to/aws-access-key-id.txt \\"
          - "    --from-file=aws-secret-access-key=/path/to/aws-secret-access-key.txt"
      when: atlantis_secret_check.stdout == "NOT_FOUND"

    - name: "Atlantis ConfigMap 생성"
      shell: |
        kubectl create configmap atlantis-config -n atlantis \
          --from-literal=AWS_REGION=ap-northeast-2 \
          --dry-run=client -o yaml | kubectl apply -f -
      register: atlantis_configmap

    - name: "Atlantis Server-side Repo Config 생성"
      shell: |
        cat <<EOF | kubectl apply -f -
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: atlantis-repo-config
          namespace: atlantis
        data:
          atlantis.yaml: |
            repos:
              - id: github.com/SeSACTHON/.*
                workflow: infrastructure-workflow
                apply_requirements:
                  - approved
                  - mergeable
                allow_custom_workflows: true
            workflows:
              infrastructure-workflow:
                plan:
                  steps:
                    - env:
                        name: SETUP_ENV
                        command: 'echo "Setting up environment..."'
                    - init
                    - run: terraform validate
                    - plan:
                        extra_args:
                          - -lock-timeout=5m
                          - -var-file=terraform.tfvars
                    - run: |
                        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                        echo "✅ Terraform Plan Complete"
                        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                apply:
                  steps:
                    - env:
                        name: SETUP_ENV
                        command: 'echo "Setting up environment..."'
                    - init
                    - apply:
                        extra_args:
                          - -lock-timeout=5m
                          - -var-file=terraform.tfvars
                    - run: |
                        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                        echo "📊 Exporting Terraform Outputs (Phase 3)..."
                        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                        terraform output -json > /tmp/tf-outputs.json
                        terraform output -raw ansible_inventory > /tmp/ansible-inventory.ini
                        if command -v kubectl &> /dev/null; then
                            kubectl get namespace argocd &>/dev/null || kubectl create namespace argocd
                            kubectl create configmap terraform-outputs \
                              --from-file=tf-outputs.json=/tmp/tf-outputs.json \
                              --from-file=ansible-inventory.ini=/tmp/ansible-inventory.ini \
                              --namespace=argocd \
                              --dry-run=client -o yaml | kubectl apply -f -
                            echo "✅ Terraform Outputs saved to ConfigMap (namespace: argocd)"
                            kubectl get configmap terraform-outputs -n argocd
                            echo "🔄 ArgoCD PreSync Hook이 이 ConfigMap을 읽어서 Ansible을 실행합니다"
                        else
                            echo "⚠️  kubectl not found, skipping ConfigMap creation"
                    - run: |
                        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                        echo "🔗 Triggering Post-Apply Actions..."
                        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                        echo "Terraform Apply Complete!"
        EOF
      register: atlantis_repo_config

    - name: "Atlantis Deployment 파일 복사 (Master 노드로)"
      copy:
        src: "{{ playbook_dir }}/../../k8s/atlantis/atlantis-deployment.yaml"
        dest: /tmp/atlantis-deployment.yaml
        mode: '0644'
      when: atlantis_secret_check.stdout != "NOT_FOUND"

    - name: "기존 StatefulSet 삭제 (업데이트를 위해)"
      shell: |
        kubectl delete statefulset atlantis -n atlantis --ignore-not-found=true
      register: atlantis_statefulset_delete
      when: atlantis_secret_check.stdout != "NOT_FOUND"
      changed_when: "'deleted' in atlantis_statefulset_delete.stdout or 'not found' in atlantis_statefulset_delete.stdout"

    - name: "StatefulSet 삭제 대기"
      shell: |
        kubectl wait --for=delete statefulset/atlantis -n atlantis --timeout=60s || true
      when: atlantis_secret_check.stdout != "NOT_FOUND" and atlantis_statefulset_delete.changed
      ignore_errors: true

    - name: "Atlantis Deployment 적용"
      shell: |
        kubectl apply -f /tmp/atlantis-deployment.yaml
      register: atlantis_deployment
      when: atlantis_secret_check.stdout != "NOT_FOUND"

    - name: "Atlantis Pod Ready 대기"
      shell: |
        kubectl wait --for=condition=ready pod -l app=atlantis -n atlantis --timeout=300s || true
        kubectl get pods -n atlantis -l app=atlantis
        kubectl describe pod -l app=atlantis -n atlantis | tail -20
      register: atlantis_pod_wait
      when: atlantis_deployment is defined and atlantis_deployment.changed
      ignore_errors: true
      changed_when: false

    - name: "Atlantis Pod 상태 확인"
      shell: |
        # Pod 상태 확인 (CrashLoopBackOff도 감지)
        kubectl get pods -n atlantis -l app=atlantis -o jsonpath='{.items[0].status.containerStatuses[0].state.waiting.reason}{.items[0].status.containerStatuses[0].state.running.startedAt}{.items[0].status.phase}'
      register: atlantis_pod_status_raw
      when: atlantis_deployment is defined and atlantis_deployment.changed
      changed_when: false
      failed_when: false

    - name: "Atlantis Pod 실제 상태 파싱"
      set_fact:
        atlantis_pod_status: "{{ 'CrashLoopBackOff' if 'CrashLoopBackOff' in atlantis_pod_status_raw.stdout else ('Running' if 'Running' in atlantis_pod_status_raw.stdout or atlantis_pod_status_raw.stdout == '' else atlantis_pod_status_raw.stdout) }}"
      when: atlantis_deployment is defined and atlantis_deployment.changed

    - name: "Atlantis Pod 로그 확인 (에러 시)"
      shell: |
        kubectl logs -n atlantis -l app=atlantis --tail=50 || echo "로그를 가져올 수 없습니다"
      register: atlantis_pod_logs
      when: atlantis_deployment is defined and atlantis_deployment.changed and atlantis_pod_status != "Running"
      changed_when: false

    - name: "Atlantis Pod 상태 출력"
      debug:
        msg:
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - "📊 Atlantis Pod 상태: {{ atlantis_pod_status }}"
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - ""
          - "{{ atlantis_pod_logs.stdout_lines | default([]) }}"
      when: atlantis_deployment is defined and atlantis_deployment.changed

    - name: "Atlantis Pod 실패 시 경고"
      fail:
        msg:
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - "❌ Atlantis Pod가 정상적으로 시작되지 않았습니다!"
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - ""
          - "현재 상태: {{ atlantis_pod_status }}"
          - ""
          - "로그 확인:"
          - "{{ atlantis_pod_logs.stdout_lines | default(['로그를 가져올 수 없습니다']) }}"
          - ""
          - "수동 확인:"
          - "  kubectl describe pod -n atlantis -l app=atlantis"
          - "  kubectl logs -n atlantis -l app=atlantis"
      when: 
        - atlantis_deployment is defined and atlantis_deployment.changed
        - atlantis_pod_status != "Running"

    - name: "Atlantis Service를 NodePort로 변경 (ALB target-type: instance 필수)"
      shell: |
        kubectl patch svc atlantis -n atlantis -p '{"spec":{"type":"NodePort","ports":[{"name":"http","port":80,"protocol":"TCP","targetPort":4141,"nodePort":{{ atlantis_node_port }}}]}}'
      register: atlantis_service_patch
      changed_when: "'patched' in atlantis_service_patch.stdout or 'unchanged' in atlantis_service_patch.stdout"
      when: atlantis_deployment is defined and atlantis_deployment.changed

    - name: "Atlantis Service 확인"
      shell: |
        kubectl get svc atlantis -n atlantis
      register: atlantis_service
      changed_when: false
      when: atlantis_secret_check.stdout != "NOT_FOUND"

    - name: "Atlantis Ingress 확인 (14-nodes-ingress.yaml에 포함됨)"
      shell: |
        kubectl get ingress atlantis-ingress -n atlantis 2>/dev/null || echo "Ingress 없음 (07-ingress-resources.yml에서 생성 예정)"
      register: atlantis_ingress_check
      changed_when: false
      when: atlantis_secret_check.stdout != "NOT_FOUND"

    - name: "Phase 3: RBAC 권한 확인"
      shell: |
        kubectl get clusterrole atlantis-configmap-creator 2>/dev/null && \
        kubectl get clusterrolebinding atlantis-configmap-creator 2>/dev/null && \
        echo "✅ RBAC 권한 설정 완료" || echo "⚠️  RBAC 권한 확인 필요"
      register: rbac_check
      changed_when: false
      when: atlantis_deployment is defined and atlantis_deployment.changed

    - name: "Phase 3: SSH Key Secret 생성 안내"
      debug:
        msg:
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - "🔐 Phase 3: SSH Key Secret 생성 필요"
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - ""
          - "ArgoCD Hooks에서 Ansible이 SSH로 접속하기 위해 Secret이 필요합니다:"
          - ""
          - "생성 명령어:"
          - "  ./scripts/utilities/create-ssh-key-secret.sh"
          - ""
          - "또는 수동 생성:"
          - "  kubectl create secret generic k8s-cluster-ssh-key \\"
          - "    --from-file=ssh-privatekey=~/.ssh/id_rsa \\"
          - "    --namespace=argocd"
      when: atlantis_deployment is defined and atlantis_deployment.changed

    - name: "Atlantis 정보 출력"
      debug:
        msg:
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - "✅ Atlantis 배포 완료 (Phase 3)"
          - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          - ""
          - "📍 배포 위치:"
          - "  - Namespace: atlantis"
          - "  - Node: k8s-monitoring (정확한 노드 지정: nodeAffinity)"
          - "  - Pod: atlantis-0"
          - ""
          - "🌐 접속 정보:"
          - "  - URL: https://atlantis.growbin.app"
          - "  - Health Check: https://atlantis.growbin.app/healthz"
          - ""
          - "🔧 Phase 3 기능:"
          - "  - ✅ kubectl 설치 (Init Container)"
          - "  - ✅ RBAC 권한 (ConfigMap 생성)"
          - "  - ✅ Terraform Outputs → ConfigMap 자동 저장"
          - "  - ✅ ArgoCD Hooks 연계 준비 완료"
          - ""
          - "📋 다음 단계:"
          - "  1. SSH Key Secret 생성:"
          - "     ./scripts/utilities/create-ssh-key-secret.sh"
          - ""
          - "  2. GitHub Webhook 설정:"
          - "     - Repository Settings → Webhooks → Add webhook"
          - "     - Payload URL: https://atlantis.growbin.app/events"
          - "     - Content type: application/json"
          - "     - SSL verification: ✅ Enable SSL verification"
          - "     - Secret: (위에서 생성한 github-webhook-secret)"
          - "     - Events: Pull requests, Pushes"
          - ""
          - "  3. ArgoCD Application 배포:"
          - "     kubectl apply -f argocd/application-14nodes-with-hooks.yaml"
          - ""
          - "  4. 테스트 PR 생성:"
          - "     - terraform/ 디렉토리 수정"
          - "     - PR 생성 → 자동으로 'atlantis plan' 실행"
          - "     - PR 코멘트에 'atlantis apply' 입력 → Apply 실행"
          - "     - ConfigMap 자동 생성 확인"
          - "     - ArgoCD Sync → PreSync Hook (Ansible) 자동 실행"
          - ""
          - "{{ atlantis_service.stdout_lines }}"
      when: atlantis_deployment is defined and atlantis_deployment.changed
