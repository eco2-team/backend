apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: location-ingress
  namespace: location
spec:
  rules:
  - host: api.dev.growbin.app
    http:
      paths:
      - path: /api/v1/locations/
        pathType: Prefix
        backend:
          service:
            name: location-api
            port:
              number: 8000
