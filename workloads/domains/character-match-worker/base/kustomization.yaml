apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: character

commonLabels:
  app.kubernetes.io/name: character-match-worker
  app.kubernetes.io/component: worker
  app.kubernetes.io/part-of: eco2

resources:
- deployment.yaml
- configmap.yaml
