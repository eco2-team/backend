apiVersion: v1
kind: Service
metadata:
  name: my-grpc
  namespace: my
  labels:
    app: my-grpc
spec:
  type: ClusterIP
  selector:
    app: my-grpc
  ports:
  - port: 50052
    targetPort: 50052
    protocol: TCP
    name: grpc
