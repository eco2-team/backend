apiVersion: v1
kind: Service
metadata:
  name: my-api
  namespace: my
  labels:
    app: my-api
    tier: business-logic
    domain: my
spec:
  type: ClusterIP
  selector:
    app: my-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
