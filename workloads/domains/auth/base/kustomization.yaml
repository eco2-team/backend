apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: auth
resources:
- deployment.yaml
- service.yaml
- configmap.yaml
# ingress는 별도 관리를 위해 제외 (ALB 설정 복잡도 때문)
# - ingress.yaml
- db-bootstrap-job.yaml
labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/part-of: ecoeco-backend
    domain: auth
    tier: business-logic
