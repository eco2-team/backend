apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-api
  namespace: location
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: location-api
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi

