apiVersion: v1
kind: Service
metadata:
  name: location-api
  namespace: location
  labels:
    app: location-api
    tier: business-logic
    domain: location
spec:
  type: NodePort
  selector:
    app: location-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http

