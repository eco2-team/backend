apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-api
  namespace: location
spec:
  template:
    spec:
      containers:
      - name: location-api
        env:
        - name: POSTGRES_HOST
          value: dev-postgresql.postgres.svc.cluster.local
