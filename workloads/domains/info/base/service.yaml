apiVersion: v1
kind: Service
metadata:
  name: info-api
  namespace: info
  labels:
    app: info-api
spec:
  selector:
    app: info-api
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
