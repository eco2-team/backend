apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: chat

resources:
- ../base

patches:
- path: patch-deployment.yaml
  target:
    kind: Deployment
    name: chat-worker

commonLabels:
  environment: dev
