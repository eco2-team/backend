apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: chat

resources:
- deployment.yaml
- configmap.yaml
- hpa.yaml

commonLabels:
  app.kubernetes.io/name: chat-worker
  app.kubernetes.io/component: worker
  app.kubernetes.io/part-of: eco2
