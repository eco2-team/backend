apiVersion: v1
kind: Service
metadata:
  name: sse-gateway
  namespace: sse-consumer
  labels:
    app: sse-gateway
    tier: integration
    domain: sse
spec:
  type: ClusterIP
  selector:
    app: sse-gateway
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
