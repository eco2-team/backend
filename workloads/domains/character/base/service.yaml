apiVersion: v1
kind: Service
metadata:
  name: character-api
  namespace: character
  labels:
    app: character-api
    tier: business-logic
    domain: character
spec:
  type: NodePort
  selector:
    app: character-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http

