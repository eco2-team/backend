apiVersion: v1
kind: ConfigMap
metadata:
  name: character-config
  namespace: character
data:
  LOG_LEVEL: INFO
  ENVIRONMENT: base
  DEBUG: 'true'
  # CHARACTER_DATABASE_URL is managed by character-secret (ExternalSecret)
