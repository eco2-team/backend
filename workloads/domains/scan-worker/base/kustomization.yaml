apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: scan

resources:
- deployment.yaml
- configmap.yaml
# HPA removed - managed by workloads/scaling (KEDA ScaledObject)
# - hpa.yaml

commonLabels:
  app.kubernetes.io/name: scan-worker
  app.kubernetes.io/component: worker
  app.kubernetes.io/part-of: ecoeco-backend
