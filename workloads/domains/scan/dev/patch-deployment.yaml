apiVersion: apps/v1
kind: Deployment
metadata:
  name: scan-api
  namespace: scan
spec:
  template:
    spec:
      containers:
      - name: scan-api
        env:
        - name: POSTGRES_HOST
          value: dev-postgresql.postgres.svc.cluster.local
