apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: scan
resources:
- deployment.yaml
- service.yaml
- configmap.yaml
# HPA removed - managed by workloads/scaling (Prometheus Adapter based)
# - hpa.yaml
labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/part-of: ecoeco-backend
    domain: scan
    tier: business-logic
