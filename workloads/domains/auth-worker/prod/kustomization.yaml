apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: auth
resources:
- ../base
labels:
- includeSelectors: true
  pairs:
    environment: prod
images:
- name: docker.io/mng990/eco2
  newTag: auth-worker-prod-latest
patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 2
  target:
    kind: Deployment
    name: auth-worker
