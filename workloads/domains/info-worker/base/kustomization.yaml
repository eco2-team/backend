apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: info

resources:
- deployment.yaml

commonLabels:
  app.kubernetes.io/name: info-worker
  app.kubernetes.io/component: worker
  app.kubernetes.io/part-of: ecoeco-backend
