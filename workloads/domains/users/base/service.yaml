apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: users
  labels:
    app: users-api
    domain: users
spec:
  selector:
    app: users-api
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
