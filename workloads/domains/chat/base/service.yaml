apiVersion: v1
kind: Service
metadata:
  name: chat-api
  namespace: chat
  labels:
    app: chat-api
    tier: business-logic
    domain: chat
spec:
  type: NodePort
  selector:
    app: chat-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
