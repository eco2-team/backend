apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prometheus

resources:
# Cluster Overview Dashboard (25 Nodes)
- cluster-all-nodes.yaml
# Master Node Dashboards
- master-node-resources.yaml
- master-control-plane.yaml
- master-downgrade-decision.yaml
# Worker Nodes Dashboard
- worker-nodes-resources.yaml
# Domain API Dashboards
- domain-ext-authz.yaml
- domain-scan-api.yaml
- domain-character.yaml
# SSE Pipeline Dashboard
- scan-sse-pipeline.yaml
# Event Bus Layer Dashboard
- event-bus-layer.yaml
# Infrastructure Dashboards
- rabbitmq.yaml
- redis-cluster.yaml

commonLabels:
  monitoring: ecoeco-cluster
  managed-by: kustomize
