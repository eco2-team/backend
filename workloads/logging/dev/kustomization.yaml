apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: logging

resources:
- ../base
# NetworkPolicy는 workloads/network-policies/에서 별도 ArgoCD App으로 배포

commonLabels:
  environment: dev
