apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: logging

resources:
- elasticsearch.yaml
- kibana.yaml
- fluent-bit.yaml
# network-policy.yaml은 별도 kustomization으로 분리 (commonLabels 적용 제외)

commonLabels:
  app.kubernetes.io/part-of: ecoeco-observability
  tier: observability
