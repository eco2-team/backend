apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kibana Saved Objects as CRDs (eck-custom-resources)
# 선언적 관리: kubectl apply로 대시보드 배포
resources:
- dataview.yaml
- overview-dashboard.yaml
- debug-dashboard.yaml
- business-dashboard.yaml

commonLabels:
  app.kubernetes.io/part-of: ecoeco-observability
  app.kubernetes.io/managed-by: kustomize
